<h2>{{artist.name}} <a href="/artists/{{artist.id}}/edit">(edit)</a></h2>
<h4>{{artist.nationality}}</h4>

<img class='artist-photo' src="{{artist.photoUrl}}">

<h3>Songs</h3>
<ul>
  {{#each songs}}
    <!-- build the list element for each song -->
    <li>
      <a href="/songs/{{id}}">
        {{title}} ({{album}})
      </a>
    </li>

  {{/each}}
</ul>
